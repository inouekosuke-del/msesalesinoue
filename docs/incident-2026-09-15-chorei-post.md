# 障害記録 2026-09-15 ｜ 朝礼がSlackに流れない

**症状** — セールスチームが朝礼ボードに投稿しても、
`#group-セールス朝礼終礼`（C0BTCHCN8B1）に反映されない。
2026-09-15 09:48 に竹内さんから指摘。

**影響範囲** — 朝礼（AM）のみ。終礼（PM）と自動投稿は正常。

## 切り分け結果

| 確認したこと | 結果 |
|---|---|
| 朝礼の入力はDBに保存されているか | **されている**。朝礼シートに 9/14 は5件、9/15 は7件（inoue 8:31 / uematsu 9:20 / takeuchi 9:21 / ijuin 9:24 / hirose 9:30 / okamoto 9:35 ほか） |
| Slackに朝礼投稿が届いているか | **届いていない**。最後の朝礼投稿は 9/11 09:38 の「チーム全員」ダイジェスト。個人投稿は 9/10 14:07（伊集院）が最後 |
| 終礼は動いているか | **動いている**。9/14 は 17:59〜18:48 の入力が 18:00〜18:49 にそのまま投稿されている |
| 他の定期実行は動いているか | **動いている**。`ingestDone_2026-09-14_07:30`、`ordersPostedAt 2026-09-15 8:29:45`（＝「本日の指示」投稿）が記録されている |
| 状態キー（MSE朝礼ボード_データ の設定シート） | AM側だけが 9/11 で止まっている。`amDigest_2026-09-11` / `amPosted_2026-09-11` が最後で、9/14・9/15 のAMキーは**存在しない**。一方 `pmPosted_2026-09-14` は書かれている |
| 投稿先チャンネルが消えていないか | 消えていない。チャンネルは 9/10 10:26 に `group-mse朝礼終礼` → `group-セールス朝礼終礼` へ改名されたが、IDは C0BTCHCN8B1 のまま |

**結論** — 朝礼の保存は成功し、Slack投稿（個人投稿・9:30ダイジェストの両方）だけが実行されていない。
停止したのは **2026-09-11 09:38 〜 2026-09-14 09:22 の間**。終礼・取込・指示投稿は無傷なので、
アプリ全体ではなく **AM系の処理だけ**が落ちている。

## 次にやること（GAS側。ここからは所有者のみ実行可）

リモートセッションからは Apps Script のソースもトリガー一覧も取得できない
（Drive API はGASソースを返さない。`MSE朝礼ボード_ソース` フォルダは空のまま）。
Apps Script エディタを開いて上から順に確認する：

1. **トリガー一覧** — AM系（9:25 アラート / 9:30 ダイジェスト・投稿）のトリガーが消えていないか。
   GASは連続失敗したトリガーを自動で無効化する。PM系が残っていてAM系だけ無い場合はこれが原因
2. **実行数（executions）** — 9/14 09:30 前後のAM関数の実行ログ。例外が出ていればそこが原因
3. **9/11夕〜9/14朝の変更履歴** — 停止した時間帯に入った改修を疑う

## 併せて直すべき設定の腐り

`MSE朝礼ボード_データ` の設定シートに `slackChannel = group-mse-朝礼` が残っている。
**この名前のチャンネルはSlackに存在しない**（実体は `group-セールス朝礼終礼` / C0BTCHCN8B1）。
名前解決でこの値を使う経路があれば `channel_not_found` で落ちる。
今回の直接原因かどうかに関わらず、**チャンネルIDでの指定に直す**こと。

## この障害が示していること

`README.md` の未着手課題1「GASソースのGit取り込み」が未了のため、
**症状は1時間で特定できたのに、コードが読めず原因確定も修正もできなかった**。
`docs/gas-backup-runbook.md` をローカルで実行する優先度を上げる。
