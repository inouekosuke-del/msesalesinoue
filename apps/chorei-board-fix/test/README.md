# ローカル回帰テスト

GASのスタブを噛ませて、`ChoreiFix.gs` / `ChoreiAgg.gs` を Node で実行する。
本番のスプレッドシートに触る前に、手元で結果を確認するためのもの。

## データの用意

案件データは会社名・金額を含むのでGitには入れていない。実行前に自分で書き出す。

`MSE朝礼ボード_データ` の各シートを
**ファイル > ダウンロード > カンマ区切り形式（.csv）** で保存し、
`test/sheets/blockNN.csv` として置く。NN は `harness.js` の `NAMES` の対応表を見ること
（例: 案件シート → `block00.csv`、members → `block18.csv`）。
1行目がヘッダー行であること。

## 実行

```bash
cd apps/chorei-board-fix
cp ChoreiFix.gs test/ChoreiFix.js
cp ChoreiAgg.gs test/ChoreiAgg.js
cd test && node harness.js
```

## 確認すること

- `修復_ドライラン()` の直後に「書き込み回数: 0」と出ること（ドライランで書かない）
- 回帰テストの「再実行で残る書き換え: 0」（何度流しても同じ結果になる＝冪等）
- 回帰テストで「月キー」「名寄せ」の件数がすべて 0 になること

2026-09-11 時点のデータでの実測値:

| | 修復前 | 修復後 |
|---|---:|---:|
| ゼロ埋めされていない月キー | 22セル | 0 |
| 案件の ownerId 空 | 2行 | 0 |
| 日次履歴の memberId 空 | 78行 | 0 |
| 2026-09 計上見込み | ￥600,000 | ￥1,300,000 |
| 2026-11 吉牟田の受注見込み | 担当不明で欠落 | ￥8,660,000 |
